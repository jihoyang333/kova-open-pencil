# W4 Cluster 09 + 10 Fix Implementation Plan

> **For agentic workers:** Executed inline. Spec-doc only (PRDs + Plans markdown). No app code touched. One commit per finding.

**Goal:** Close 27 QA findings — 18 Cluster 09 (snapshots + version history + trash) + 9 Cluster 10 (AI chat + memory).

**Architecture:** PRD + Plan markdown edits only. Commits land on `fix/qa-w4-cluster-09-10-snapshots-ai` off `feat/m9-shopify` HEAD. No worktree.

**Branch:** `fix/qa-w4-cluster-09-10-snapshots-ai`
**Base:** `feat/m9-shopify` @ `33722da7` (Merge W3 Canvas Bundle)

---

## Conventions

- Commit format: `fix(qa-w4-c09): <ID> <summary>` or `fix(qa-w4-c10): <ID> <summary>`
- Stage explicitly: `git add <path>` — never `git add .`
- Scope: PRD 09, PRD 10, Plan 09, Plan 10 only. Cross-cluster doc edits limited to documentation/handshake notes (no Plan 02 / Plan 06 / Plan 11 edits unless explicitly required).

---

## Cluster 09 Findings (18)

### Task C09-1 — B-CRIT13 signInAs helper rewrite

**Target:** `docs/kova-final-impl-plans/09-version-history-and-trash-plan.md` lines 355-367.

Replace magic-link Bearer pattern with `signInWithPassword` + `auth.admin.updateUserById`. Re-run note: tests should fail before fix, pass after.

Commit: `fix(qa-w4-c09): B-CRIT13 signInAs helper uses signInWithPassword`

---

### Task C09-2 — CT-013 SECURITY DEFINER search_path verify

**Target:** PRD 09 §4.1 RPCs at lines 252, 316, 366, 399.

State check: each RPC already has `SET search_path = public, pg_temp`. PRD §11.5 line 520 calls out CT-013 as "fix agent adds during W4 pass." Add cross-reference + close-out note to PRD §11.5.

Commit: `fix(qa-w4-c09): CT-013 verify search_path lock on all 4 SECURITY DEFINER RPCs`

---

### Task C09-3 — C-HIGH7 initial_state_blob_path column + hydrate

**Target:** Plan 09 Task 19 (lines 2305-2428). Also new migration block in Plan 09 Task 1.

Add:
- Migration: `ALTER TABLE public.canvases ADD COLUMN initial_state_blob_path text;`
- Plan 09 Task 19: on duplicate, set `initial_state_blob_path` on the new canvas row via RPC.
- Plan 09 §6: document Cluster 02 contract (canvas open reads column → hydrates Yjs from blob if present).
- Cross-cluster note: Plan 02 amendment follow-up needed (out of scope this PR).

Commit: `fix(qa-w4-c09): C-HIGH7 initial_state_blob_path column + hydrate contract`

---

### Task C09-4 — C-HIGH8 claim_snapshots_for_prune RPC + claimed_at

**Target:** Plan 09 Task 20 (lines 2432-2546).

Add migration: `ALTER TABLE public.canvas_snapshots ADD COLUMN claimed_at timestamptz;` + `CREATE OR REPLACE FUNCTION public.claim_snapshots_for_prune` SECURITY DEFINER + `SET search_path = public, pg_temp` + `FOR UPDATE SKIP LOCKED`.

Update cron handler to call `claim_snapshots_for_prune` RPC instead of inline `.select().lt().limit()`.

Commit: `fix(qa-w4-c09): C-HIGH8 claim_snapshots_for_prune RPC with SKIP LOCKED`

---

### Task C09-5 — C-HIGH9 7th-day Storage sweep

**Target:** Plan 09 Task 20 line 2507 ("Optional 7th-day sweep" placeholder).

Replace placeholder with full sweep block:
- Cron entry weekly `0 5 * * 0` (Sunday 5am UTC) in vercel.json.
- Logic: list `canvas-snapshots/` bucket → diff against `canvas_snapshots.scene_blob_path` rows → delete orphan objects.

Commit: `fix(qa-w4-c09): C-HIGH9 7th-day Storage sweep cron`

---

### Task C09-6 — B-HIGH20 close via B-CRIT13

**Target:** Plan 09 — add note after Task 1 helper block.

Add explicit close-out: "B-HIGH20 inline JWT pattern resolved by B-CRIT13 signInAs rewrite (C09-1)."

Commit: `fix(qa-w4-c09): B-HIGH20 close-out note (resolved by B-CRIT13)`

---

### Task C09-7 — C-MED22 wire verifyIdempotency

**Target:** Plan 09 Task 19 lines 2356-2359, 2405-2406.

Replace commented stubs with real `verifyIdempotency` import + invocation from `@/api/_shared/idempotency` (Plan 11 Task 1.3).

Commit: `fix(qa-w4-c09): C-MED22 wire verifyIdempotency in duplicate-to-canvas`

---

### Task C09-8 — C-MED23 snapshot_id Storage path

**Target:** Plan 09 Task 19 line 2390.

Pre-generate `snapshotId = crypto.randomUUID()` BEFORE Storage upload. Use as both Storage path component and `create_snapshot` row id.

Commit: `fix(qa-w4-c09): C-MED23 use snapshot_id in Storage path`

---

### Task C09-9 — C-MED24 retention constant import

**Target:** Plan 09 Task 20 line 2486.

Replace `30 * 24 * 60 * 60 * 1000` with `SNAPSHOT_FREE_RETENTION_DAYS * 24 * 60 * 60 * 1000` imported from `@/config/feature-flags`. Add feature-flags entry block.

Commit: `fix(qa-w4-c09): C-MED24 SNAPSHOT_FREE_RETENTION_DAYS constant import`

---

### Task C09-10 — C-MED25 useCanvasEditLock single-owner

**Target:** Plan 09 Task 12a lines 1546-1559.

Replace `lockCount` ref-count with boolean `isLocked`. On double-lock attempt, log warning + Sentry capture. Update tests.

Commit: `fix(qa-w4-c09): C-MED25 useCanvasEditLock single-owner boolean`

---

### Task C09-11 — C-MED26 verify version-history menu wire

**Target:** Plan 09 Task 21 (line 2547). Reference Cluster 06 W3 File-menu landing.

Add §11 cross-cuts note documenting handshake: Cluster 06 W3 ships File→Version-history menu item that emits `open-version-history`; Plan 09 Task 21 SnapshotTimelinePanel mounts on that event.

Commit: `fix(qa-w4-c09): C-MED26 document File-menu version-history handshake`

---

### Task C09-12 — A-MED6 PRD 09 status bump

**Target:** PRD 09 §0 lines 5-15.

Status: `DRAFT` → `IN-REVIEW`. Last updated: `2026-05-15` → `2026-05-19`.

Commit: `fix(qa-w4-c09): A-MED6 PRD 09 status IN-REVIEW 2026-05-19`

---

### Task C09-13 — B-MED9 PRD 09 path drift

**Target:** Plan 09 lines 5, 12, 13, 15, 152, 1120, 2780, 2785, 2786.

Replace `docs/prd/` → `docs/kova-final-prds/`. Replace `09-version-history-and-trash.md` references accordingly.

Commit: `fix(qa-w4-c09): B-MED9 PRD path drift docs/prd → docs/kova-final-prds`

---

### Task C09-14 — B-MED5 Promise.all (09 portion)

**Target:** Plan 09 Task 19 lines ~2367-2387 (sequential awaits for snapshot row + blob download).

Audit + parallelize where awaits are independent. Snapshot fetch + admin-client setup do not block — but blob download depends on snap.scene_blob_path, so cannot parallelize those two. Look for genuinely independent awaits.

Commit: `fix(qa-w4-c09): B-MED5 parallelize independent awaits Plan 09 portion`

---

### Task C09-15 — C-LOW09.9 edge runtime config

**Target:** Plan 09 Task 19 + Task 20 Edge Function bodies.

Add `export const config = { runtime: 'edge' }` to each Vercel Function source block.

Commit: `fix(qa-w4-c09): C-LOW09.9 add edge runtime config to Vercel Functions`

---

### Task C09-16 — C-LOW09.10 CSS overlay refactor

**Target:** Plan 09 Task 21 lines 2596-2625.

Refactor to CSS-driven `pointer-events`. Remove `window.__spaceHeld` global; replace with composable `useSpacePan` ref check.

Commit: `fix(qa-w4-c09): C-LOW09.10 CSS pointer-events overlay (remove __spaceHeld)`

---

### Task C09-17 — C-LOW09.11 format_version CI grep

**Target:** Plan 09 + Plan 07a (Plan 07a already covers via W3; coordinate).

Add CI step description to Plan 09 §10 (test plan): grep for `format_version` bump in `packages/core/codec/` diffs.

Commit: `fix(qa-w4-c09): C-LOW09.11 format_version CI grep step`

---

### Task C09-18 — C-LOW09.12 KovaSkeleton branch

**Target:** Plan 09 Task 17 lines 2186-2208.

Add `<KovaSkeleton v-if="store.loadingByCanvas[canvasId]">` rendering branch in SnapshotTimelinePanel template.

Commit: `fix(qa-w4-c09): C-LOW09.12 render KovaSkeleton while loading`

---

## Cluster 10 Findings (9)

### Task C10-1 — CT-005 AI default-active verify

**Target:** PRD 10 lines 24, 39, 56, 147, 740.

Grep verify zero "Design (default-active)" occurrences. Replace any remaining with "AI (default-active)".

Commit: `fix(qa-w4-c10): CT-005 verify AI default-active in PRD 10`

---

### Task C10-2 — CT-004 addMeasurement signature

**Target:** PRD 10 line 12 (depends-on line still says "Slice + Measurement NodeTypes") + Plan 10 Task 11 signature.

Line 12: change "Slice + Measurement NodeTypes" → "Slice NodeType + page-level Measurement methods (07a §7.1b — Measurement is NOT a NodeType)".

Plan 10 Task 11: confirm `addMeasurement` signature matches PRD 10 §6.3.4 line 584-611 + PRD 07a §2.1.

Commit: `fix(qa-w4-c10): CT-004 addMeasurement page-level signature + line 12 phrasing`

---

### Task C10-3 — C-HIGH10 useRightPanelStore canonical verify

**Target:** Plan 10 lines 2394, 2401, 2436.

Grep verify zero `useRightPanelTabStore` or `right-panel-tab` (store name) occurrences. Already canonicalized per W0-3 (line 2437 documents the lock). Note: `right-panel-tab-design`/`right-panel-tab-ai` data-test-ids are valid (DOM selectors, not store names).

Commit: `fix(qa-w4-c10): C-HIGH10 verify useRightPanelStore canonical in Plan 10`

---

### Task C10-4 — C-MED27 dual mutation refactor

**Target:** Plan 10 Task 3 (`updateProductReferences` action) + Task 4 (`persistAndPatch`).

Refactor Task 4 to call Task 3's `updateProductReferences` action instead of direct mutation. Single mutation entry point.

Commit: `fix(qa-w4-c10): C-MED27 single mutation entry for product_references`

---

### Task C10-5 — C-MED28 chip row BELOW attachments

**Target:** PRD 10 line 635 (§6.4.2): "chip row above the existing attachment-thumbnail row".

Change to "chip row BELOW the attachment-thumbnail row" to match §3.2 line 158 + §12.12 founder lock item 7.

Commit: `fix(qa-w4-c10): C-MED28 chip row BELOW attachments (match §3.2/§12.12 lock)`

---

### Task C10-6 — C-MED-10.6 §8.5/8.6 verification

**Target:** Plan 10 Task 19 (or relevant acceptance task).

Add explicit acceptance step verifying §8.5 server-side proxy + §8.6 sub-processor disclosure wiring.

Commit: `fix(qa-w4-c10): C-MED-10.6 §8.5/8.6 acceptance verification`

---

### Task C10-7 — B-MED5 Promise.all (10 portion)

**Target:** Plan 10 chat-side awaits.

Audit + parallelize independent awaits. Pair with C09-14 (B-MED5 shared finding).

Commit: `fix(qa-w4-c10): B-MED5 parallelize independent awaits Plan 10 portion`

---

### Task C10-8 — A-LOW6 PRD 10 status bump

**Target:** PRD 10 §0 lines 7, 11.

Status: `DRAFT 2026-05-15` → `IN-REVIEW 2026-05-17` (line 7). Last updated: `2026-05-15` → `2026-05-17` (line 11).

Commit: `fix(qa-w4-c10): A-LOW6 PRD 10 status IN-REVIEW 2026-05-17`

---

### Task C10-9 — C-LOW10.5 engine_unavailable + AI default E2E

**Target:** PRD 10 §8.4 + Plan 10 Tasks 10/11 + Task 16 (E2E).

(a) Reconcile `engine_unavailable` error code — add to PRD §8.4 acceptance.
(b) Update Plan 10 Task 16 E2E to assert AI tab visible by default.

Commit: `fix(qa-w4-c10): C-LOW10.5 engine_unavailable in §8.4 + AI default E2E`

---

## Final task — Quality gates + push

```sh
# From kova-open-pencil-1/
git log --oneline feat/m9-shopify..HEAD | wc -l       # expect 27 (one per finding) + possible verify-only no-ops
grep -nE "vi\.mock|e\.key|Math\.random" docs/kova-final-prds/09*.md docs/kova-final-prds/10*.md docs/kova-final-impl-plans/09*.md docs/kova-final-impl-plans/10*.md
grep -nE "icon-lucide-" docs/kova-final-prds/09*.md docs/kova-final-prds/10*.md docs/kova-final-impl-plans/09*.md docs/kova-final-impl-plans/10*.md
grep -nE "SECURITY DEFINER" docs/kova-final-prds/09*.md docs/kova-final-impl-plans/09*.md
git push -u origin fix/qa-w4-cluster-09-10-snapshots-ai
```

Report: branch pushed, findings closed per cluster, commit count, gate output, blockers.
